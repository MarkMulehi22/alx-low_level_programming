#include <stdio.h>
#include <stdlib.h>

/**
 * main -Program to test while loop to make a table
 * Return: 0(Success)
 */
int main(void)
{
	int i = 0, ji;

	while (i < 5)
	{
		j = 0;
		while (j<3)
		{
			printf("2");
			j++;
		}
		printf("n");
		i++
	}
	return (0);
}
